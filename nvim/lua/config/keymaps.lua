--[[
CMD s (save)
CMD w (close)
CMD j (terminal)
CMD p (search files)

CMD + SHIFT + p (commandline)
CMD + SHIFT + f (find in files)

CMD k CMD c (toggle comment)
CMD k CMD w (close all)

CMD + arrows (move cursor)
OPT + arrows (move line)
SHIFT + OPT + arrows (select)
SHIFT + CMD + arrows (select)

OPT + backsoace (del word)
CMD + backspace (del line)

]]
